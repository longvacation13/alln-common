spring:
  application:
    name: ssg-alln-common

logging:
  level:
    root: DEBUG

springdoc:
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
  api-docs:
    enabled: true
    path: /v3/api-docs
